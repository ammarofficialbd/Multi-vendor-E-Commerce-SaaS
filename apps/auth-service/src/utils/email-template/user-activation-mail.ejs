<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Activate your account</title>
  <style>
    /* Basic inline-safe email styles */
    body { margin:0; padding:0; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial; background:#f4f6f8; color:#333; }
    .container { width:100%; max-width:600px; margin:24px auto; background:#ffffff; border-radius:8px; overflow:hidden; box-shadow:0 4px 20px rgba(0,0,0,0.06); }
    .header { padding:20px; text-align:center; background:linear-gradient(90deg,#0ea5a4,#06b6d4); color:#fff; }
    .logo { font-weight:700; font-size:20px; letter-spacing:0.4px; }
    .content { padding:24px; line-height:1.45; }
    .btn-wrap { text-align:center; margin:22px 0; }
    .btn { display:inline-block; text-decoration:none; padding:12px 20px; border-radius:6px; font-weight:600; background:#0ea5a4; color:#fff; }
    .muted { color:#6b7280; font-size:13px; }
    .footer { padding:16px 24px; font-size:13px; color:#6b7280; background:#fbfdff; text-align:center; }
    a.inline { color: #0ea5a4; text-decoration: none; }
    @media only screen and (max-width:420px) {
      .content { padding:16px; }
      .header { padding:16px; }
    }
  </style>
</head>
<body>
  <div class="container" role="article" aria-label="Account activation">
    <div class="header">
      <div class="logo"><%= appName || 'YourApp' %></div>
    </div>

    <div class="content">
      <p style="margin:0 0 12px 0;">Hi <strong><%= name || 'there' %></strong>,</p>

      <p style="margin:0 0 12px 0;">
        Welcome to <strong><%= appName || 'YourApp' %></strong> — please confirm your email address to activate your account.
      </p>

      <div class="btn-wrap">
        <a href="<%= activationUrl %>" class="btn" target="_blank" rel="noopener">Activate my account</a>
      </div>

      <p style="margin:0 0 12px 0;" class="muted">
        If the button above doesn't work, copy and paste the following link into your browser:
      </p>

      <p style="word-break:break-all; font-size:13px; margin:0 0 12px 0;">
        <a class="inline" href="<%= activationUrl %>" target="_blank" rel="noopener"><%= activationUrl %></a>
      </p>

      <p class="muted" style="margin:0 0 8px 0;">
        This link will expire in <%= expiryHours || 24 %> hours.
      </p>

      <hr style="border:none;border-top:1px solid #eef2f7;margin:18px 0;" />

      <p style="margin:0 0 8px 0;">
        If you didn't create an account with <%= appName || 'YourApp' %>, you can safely ignore this email.
      </p>

      <p style="margin:0;">
        Thanks,<br/>
        The <%= appName || 'YourApp' %> Team
      </p>
    </div>

    <div class="footer">
      Need help? Email us at
      <a href="mailto:<%= supportEmail || 'support@example.com' %>" class="inline"><%= supportEmail || 'support@example.com' %></a>
    </div>
  </div>

  <!-- Plain-text fallback for email clients that prefer it -->
  <div style="display:none; white-space:pre; font-family:monospace; font-size:12px;">
    Activate your account - <%= appName || 'YourApp' %>

    Hi <%= name || 'there' %>,

    Welcome to <%= appName || 'YourApp' %>. Activate your account using the link below:
    <%= activationUrl %>

    This link expires in <%= expiryHours || 24 %> hours.

    If you didn't request this, ignore this message.

    Support: <%= supportEmail || 'support@example.com' %>
  </div>
</body>
</html>
